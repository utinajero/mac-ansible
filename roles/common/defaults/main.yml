# osx/roles/common/defaults/main.yml
---
base_osx:
  # common --------------------------------
  common:
    # taps ----------------------------------
    taps:
      - homebrew/binary
    
    # packages --------------------------------
    packages:
      - rbenv
      - ruby-build
      - ansible
      - youtube-dl
      - openssl
      - pkg-config
      - libyaml
      - autoconf
      #- packer
      #- apple-gcc42
      #- brew-cask
    
    # casks --------------------------------
    #casks:
      #- dropbox 
      #- google-drive
    #remove:
      #- wireshark
      